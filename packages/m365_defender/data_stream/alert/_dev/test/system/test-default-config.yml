input: cel
service: m365-defender-alert-vulnerability-cel
vars:
  token_url: http://{{Hostname}}:{{Port}}
  client_id: xxxx
  client_secret: xxxx
  azure_tenant_id: tenant_id
data_stream:
  vars:
    url: http://{{Hostname}}:{{Port}}
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
    include_unknown_enum_members: true
    batch_size: 2
assert:
  hit_count: 4
